!!! 5
%html
  %head
    %title Innovation Resources
    = stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags

  %body
    = link_to "Log Out", session_path("current"), method: "delete", id: "log_out"
    = render 'layouts/header'
    -if flash[:notice]
      .div.alert_area
        .flash
          =flash[:notice]
    -if flash[:error]
      .div.error_area
        .flash
          =flash[:error]
    .container
      = yield
    .content
      = render 'layouts/footer'
